import { createExpressOvenRoutes } from '../src';

describe('createExpressOvenRoutes()', () => {
  it(`should get the configs via readConfigs()`, () => {
    // createExpressOvenRoutes()
  });

  it(`should set default configs in the optional config fields`, () => {
    // your test goes here
  });

  it(`should call createRouterForApiMethod() for each route`, () => {
    // your test goes here
  });

  it(`should return router with provided routes`, () => {
    // your test goes here
  });
});
